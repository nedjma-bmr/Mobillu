export class commande{
    // le numéro de la commande 
    NumCmd:number;
    // la date d'établissment de la commande
    date: Date;
    // l'identifiant du client 
    id_client:string;
    // le nom du client 
    design_client:string;
    // valeur pr voir si la commande est choisit pr l'importation
    value : boolean ; 
}